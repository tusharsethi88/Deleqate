Drop the Brand Demo Video sample clip here as:  demo.mp4

Requirements:
- Format: MP4 (H.264 video, AAC audio) — plays in all browsers.
- It autoplays MUTED and LOOPS on the SKU card, so audio isn't needed.
- Keep it short (~5-15s) and small (ideally < 5 MB) so the homepage stays fast.
- Recommended aspect: landscape or square; it's cropped to a 150px-tall card (object-fit: cover).

The card references exactly:  /img/task_holders/brand_demo_video/demo.mp4
If you use a different filename, update the <video src> in frontend/src/pages/Home.jsx.

You can delete this README once the video is in place.
