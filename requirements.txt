# Deleqate backend dependencies (Django + React migration).
# NOTE: This used to list Flask-era packages, but the backend is Django now.
# Kept in sync with backend/requirements.txt — deploy.sh installs THIS file.
django>=5.0
django-cors-headers>=4.0
werkzeug>=3.0          # password hashing + secure_filename (parity with Flask app)
python-dotenv>=1.0
requests>=2.31         # Brevo / MSG91 OTP delivery
pillow>=10.0           # hero image resize + upload content checks
pillow-heif>=0.13      # decode iPhone HEIC/HEIF uploads → converted to JPEG on save
gunicorn>=21.2         # production WSGI server
jinja2>=3.0            # render legacy pilot workflow template server-side
openai>=1.0.0          # NVIDIA LLM integration (equity research report)
playwright>=1.40.0     # HTML to PDF conversion
PyPDF2>=3.0.0          # PDF slicing for preview (legacy; pypdf preferred)
pypdf>=4.0             # PDF preview paywall (first 2 pages) + concall PDF text extraction
beautifulsoup4>=4.12   # scrape Screener.in financials/peers for equity research
ddgs>=6.0              # optional Moneycontrol/NSE fallback search (degrades gracefully)
